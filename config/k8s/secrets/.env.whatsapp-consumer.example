# Populate through the approved secret manager; never commit real values.
# This account must have only the tables and statements required by
# whatsapp_event_consumer.py: select/update on whatsapp_webhook_events and
# outbox_events; insert on ai_inference_runs and the optional communication
# timeline projection; and sequence usage for inserted rows.
DATABASE_URL=postgresql://lanai_whatsapp_consumer:REPLACE_ME@postgresql.lanai.svc.cluster.local:5432/lanai
