# Dedicated Twenty CRM integration secret.
# Copy to .env.crm.secrets outside source control and populate only with an
# approved service token for the intended environment. Never commit the copy.
TWENTY_CRM_URL=
TWENTY_CRM_API_TOKEN=
